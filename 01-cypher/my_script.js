// Main block
const encryptButton = document.getElementById("encrypt");
const decryptButton = document.getElementById("decrypt");
const scrollUp = document.getElementById("scrollUp");

function encrypt() {
    const textToEncrypt = document.getElementById("inputArea").value;
}

function decrypt() {
    const textToEncrypt = document.getElementById("inputArea").value;
}

function scrollUp() {
    scrollUp.onClick()
}

function newEncrypt() {
    const textToEncrypt = document.getElementById("inputArea").value;
}

function newDecrypt() {
    const textToEncrypt = document.getElementById("inputArea").value;
}